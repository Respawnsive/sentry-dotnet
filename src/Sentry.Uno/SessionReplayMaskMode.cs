namespace Sentry.Uno;

/// <summary>
/// Masking mode for session replay.
/// </summary>
public enum SessionReplayMaskMode
{
    /// <summary>
    /// Mask the element.
    /// </summary>
    Mask,

    /// <summary>
    /// Unmask the element.
    /// </summary>
    Unmask,
}
