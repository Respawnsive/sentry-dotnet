<Page
    x:Class="Sentry.Samples.Uno.Presentation.TracingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sentry.Samples.Uno.Presentation"
    xmlns:uen="using:Uno.Extensions.Navigation.UI"
    xmlns:um="using:Uno.Material"
    xmlns:utu="using:Uno.Toolkit.UI"
    Background="{ThemeResource BackgroundBrush}"
    NavigationCacheMode="Required">
    <ScrollViewer IsTabStop="True">
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition />
            </Grid.RowDefinitions>
            <utu:NavigationBar Content="{Binding Title}" />

            <StackPanel
                Grid.Row="1"
                Margin="16"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <TextBlock
                    HorizontalAlignment="Center"
                    Style="{StaticResource MaterialHeadline5}"
                    Text="Tracing Examples" />

                <TextBlock
                    Margin="0,8,0,16"
                    Text="Tracing helps you understand performance and distributed systems."
                    TextWrapping="Wrap" />

                <Button Command="{Binding OpenTelemetryTransaction}" Content="OpenTelemetry Transaction" />

                <Button Command="{Binding OpenTelemetryHttp}" Content="OpenTelemetry HTTP Request" />

                <Button Command="{Binding CustomTransaction}" Content="Custom Sentry Transaction" />

                <Button Command="{Binding CustomSpan}" Content="Custom Sentry Span" />

                <Button Command="{Binding NestedSpans}" Content="Nested Spans" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
